{
  "name": "public-works-department",
  "version": "0.9.0",
  "private": true,
  "description": "Systemic public works game and dependency-free browser vertical slice.",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  }
}
